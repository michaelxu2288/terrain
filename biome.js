/**
 * @typedef {{height: number, biome: string}} Biome
 * @typedef {{start:number, end:number, biome: string}} BiomeRange
 */

class BiomeGen {

    constructor(biomes, ranges) {
        this.biomes = biomes;
        this.ranges = ranges;
    }

    getBiome() {
        
    }

    getHeight() {

    }

}